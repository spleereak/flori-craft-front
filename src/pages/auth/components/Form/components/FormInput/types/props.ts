export interface FormInputProps {
  className?: string;
  placeholder: string;
  type: string;
  name: string;
  value?: string;
  // eslint-disable-next-line no-unused-vars
  onChange?: (data: any) => void;
}
